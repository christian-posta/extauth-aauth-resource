binds:
- port: 3001
  listeners:
  - routes:
    - policies:
        extAuthz:
          host: "localhost:7070"
      backends:
      - mcp:
          targets:
          - name: everything
            stdio:
              cmd: npx
              args: ["-y", "@modelcontextprotocol/server-everything"]
