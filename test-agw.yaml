binds:
- port: 3001
  listeners:
  - routes:
    - policies:
        extAuthz:
          host: "localhost:7070"
          contextExtensions:
            aauth_resource_id: "mcp-api"
      backends:
      - mcp:
          targets:
          - name: everything
            stdio:
              cmd: npx
              args: ["-y", "@modelcontextprotocol/server-everything"]
